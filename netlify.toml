[[headers]]
  for = "/*.js"
  [headers.values]
    Content-Type = "application/javascript"
[build]
  command = "npm install --legacy-peer-deps && npm install @vitejs/plugin-react && npm run build"
  publish = "dist"

[build.environment]
  NODE_ENV = "production"
